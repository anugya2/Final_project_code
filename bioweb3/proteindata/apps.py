# "All code below written by the student."
# app config file

from django.apps import AppConfig

class ProteindataConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'proteindata'
